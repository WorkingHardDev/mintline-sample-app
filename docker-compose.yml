version: "3.8"

services:
  server:
    build: ./server
    ports:
      - "3001:3001"
    networks:
      - app-network

  client:
    build: ./client
    ports:
      - "3000:80"
    depends_on:
      - server
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
